name: dt_tracker_user
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0-134.0.dev <4.0.0'

dependencies:
  firebase_auth: ^4.14.1
  firebase_core: ^2.23.0
  firebase_database: ^10.3.5
  scrollable_reorderable_navbar: ^0.0.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
